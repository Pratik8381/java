package user;

import java.util.Scanner;

public class evenNo {
    public static void main(String[] args) {

        Scanner scanner =new Scanner(System.in);
        System.out.println("Enter the input in bytes");
        byte input = scanner.nextByte();
        System.out.println("the input is in bytes"+input);

//        (N0/2==0) ? ("evenNo") : ("oddNo");

    }
}
